# Build environment for this plan's verify steps
QuestBuild: pending   # toolchain setup running; flipped to yes only after a baseline `qpm s build` succeeds
BeatSaberDir: C:\Users\notni\BSManager\BSInstances\1.40.8
